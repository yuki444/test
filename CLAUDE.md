# ユーザープロファイル

## 基本情報
- 40歳男性、神戸在住
- アクティブ・体力あり・アクティビティ好き・夜の飲みも重視
- ゴルフは趣味（80台レベル）

## コミュニケーションスタイル
- **カジュアル・ダイレクト・命令形**（敬語なし。「〜して」「〜てほしい」が標準）
- 1メッセージに複数指示を詰め込む（「XXXして、YYYは不要、ZZZ変更」）
- 短い返答を好む。説明過多・前置き長い回答は不要
- 絵文字は低頻度。フォーマルな文書では使わない
- 数字で選ぶ（「1」「2かな」）意思決定スタイル

## 期待するアウトプット
- ビジュアル・グラフィカルな資料（パンフレット・表・図）を好む
- 具体的な金額・時刻・所要時間を必ず含める
- 初回提案から詳細まで作り込む（「詳しく説明して」の往復をなくす）
- 複数案を出す場合は表形式で比較

---

# アシスタント行動原則

## 提案前の必須チェックリスト

**旅行・スケジュール系タスクでは必ずこの順序で処理する：**

1. **物理的実現可能性の検証**
   - 着時刻 + 移動時間 + 余裕バッファ = 次のアクティビティ開始時刻 が成立するか
   - ゴルフ場は**スタート30分前チェックイン**必須
   - レンタカーPU は**到着後45分**を見込む（荷物・手続きあり）
   - 帰りフライトは**90分前空港着**が必要

2. **イベント・施設情報のファクトチェック**
   - 「毎年〇月第△〇曜」等の定型表現を**実際の年度カレンダーで確認**してから記載
   - 開催日が旅程と合致するか必ず検証
   - 確認できていない情報は「未確認」と明示し、断言しない

3. **確定情報の即時全体反映**
   - フライト・ゴルフ予約・宿泊など確定情報が提供されたら**すべての関連箇所を同時に更新**
   - 部分更新は不整合を生む。コスト表・チェックリスト・スケジュール・ヒーローブロックを一括修正

## ミスのパターン（過去の指摘から）

| ミスの種類 | 具体例 | 対策 |
|-----------|--------|------|
| イベント日程の未検証提案 | 花火大会を7/25と掲載（実際は7/31） | 年度別の実際の日程を調べてから記載 |
| 施設所在地の誤り | カーリングスタジアムを「清田区」と記載（実際は豊平区月寒） | 公式サイト・地図で確認してから記載 |
| フライト時刻の創作 | 存在しない「ANA 07:30発」を掲載 | 航空会社公式サイトで確認、確定前は「未確認」と明記 |
| 移動時間の過小評価 | ニドム→支笏湖「30分」（実際は5〜10分） | Googleマップ等で実測値を確認 |
| 実現不可能なスケジュール | 15:00便なのにウニ丼・カヤックをDay3に配置 | フライト時刻から逆算して全行程の実現可否を先に検証 |
| 部分修正による不整合 | フライト変更時に空港名・コスト・チェックリストを更新し忘れ | 確定情報変更時は必ず全関連箇所を一括更新 |

## 効率的なやり取りのルール

- **「詳しく説明して」の往復ゼロを目指す**：初回提案から背景・コスト・実現可否まで含める
- **複数アクティビティを同時に提案する場合**：スケジュール全体の整合を先に確認してから出す
- **「既知か不明か」を確認してから提案**：経験済みの観光地・施設を提案しない（例：ウポポイ）
- **ユーザーが数値を提供したら即座に反映**：「コスト表に反映して」と言わせない
- **選択肢は2〜3案、表形式で比較**：長文説明より一覧で判断できる形式にする

---

# 情報提案における原則

## イベント・施設・日程・交通情報の取り扱い

- **年度・日程を必ず確認してから提案する。** 「毎年〇月第△〇曜」などの定型表現を実際のカレンダーに当てはめ、旅程と整合しているか検証すること。
- **確認できていない情報は「未確認」と明示する。** 調査なしに事実として断言しない。
- **「この時期に行ける」と書く前に、旅程の日付との整合を必ずチェックする。**
- **ソースが曖昧な情報は提案しない。** 根拠のある情報のみを記載する。

> 背景: 2026年7月24〜26日の北海道旅行プランにおいて、道新・UHB花火大会（実際は7/31開催）を旅程内（7/25）と誤って掲載した。年度別の開催日を未確認のまま提案したことによるミス。

---

# Suno AI Lyrics Integration

毎日歌詞を整理し、Suno AIで5つのスタイルの曲を自動生成するシステム。

## セットアップ

### 1. gcui-art/suno-api を自己ホスト

```bash
# Dockerでローカルに立ち上げる場合
git clone https://github.com/gcui-art/suno-api.git
cd suno-api
cp .env.example .env
# .env の SUNO_COOKIE に Suno アカウントのCookieを設定
docker-compose up -d
```

Suno CookieはブラウザのDevTools → Application → Cookies → suno.com で取得。

### 2. GitHub Secrets に設定

リポジトリの Settings → Secrets → Actions に追加:

| Secret | 値 |
|--------|---|
| `SUNO_API_URL` | 自己ホストAPIのURL (例: `https://your-server.com`) |

### 3. Python依存関係インストール

```bash
pip install -r requirements.txt
```

## 毎日の使い方

### 1. 歌詞ファイルを作成

```bash
mkdir -p lyrics/$(date +%Y-%m-%d)
cp lyrics/template/song.txt lyrics/$(date +%Y-%m-%d)/song.txt
# ファイルを編集して歌詞を書く
```

ファイル形式:
```
title: 曲のタイトル
---
[Verse 1]
歌詞...

[Chorus]
サビの歌詞...
```

### 2. 曲を生成

```bash
# 今日の歌詞で5スタイル全て生成
python scripts/generate_music.py

# 特定の日付
python scripts/generate_music.py --date 2026-05-24

# 特定スタイルだけ
python scripts/generate_music.py --styles electronic_ambient jpop_citypop
```

### 3. 結果を確認

```bash
python scripts/list_results.py          # 全日付
python scripts/list_results.py --date 2026-05-24  # 特定日
```

結果は `output/YYYY-MM-DD/results.json` に保存される。

## 5つのスタイル

| スタイル名 | ジャンル |
|-----------|---------|
| `electronic_ambient` | Electronic / Ambient |
| `jpop_citypop` | J-Pop / City Pop |
| `rock_alternative` | Rock / Alternative |
| `jazz_bossanova` | Jazz / Bossa Nova |
| `ballad` | Ballad |

スタイルの設定は `config/styles.json` で変更可能。

## 自動実行

GitHub Actions が毎日 11:00 JST に実行される。  
その日の `lyrics/YYYY-MM-DD/song.txt` が存在すれば自動で5スタイルの曲を生成し、`output/` にコミットする。

手動実行: Actions タブ → Daily Music Generation → Run workflow

## ファイル構成

```
lyrics/
  YYYY-MM-DD/
    song.txt        ← 歌詞をここに書く
  template/
    song.txt        ← テンプレート

output/
  YYYY-MM-DD/
    results.json    ← 生成された曲のURL・情報

config/
  styles.json       ← 5スタイルの設定

scripts/
  generate_music.py ← メイン生成スクリプト
  suno_client.py    ← Suno APIクライアント
  list_results.py   ← 結果一覧表示

.github/workflows/
  daily_generate.yml ← 毎日自動実行
```
